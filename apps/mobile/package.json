{
  "name": "@campus-connect/mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo 'Skipping Flutter tests (requires Flutter CLI environment)'",
    "build": "echo 'Skipping Flutter build (requires Flutter CLI environment)'"
  }
}
